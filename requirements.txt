# Web UI
streamlit==1.35.0
streamlit-authenticator==0.3.1

# Observability & Analytics
langfuse>=2.0.0           # LLM tracing — cloud + local
pandas>=2.0.0             # Analytics dashboard dataframes

# LangGraph Orchestration
langgraph==0.2.14
langchain-core>=0.2.27
langchain-groq==0.1.6

# AI Clients
groq==0.9.0
anthropic        # Optional: Claude API

# PDF Processing
PyMuPDF==1.24.5          # fitz — fast, accurate PDF parser
reportlab==4.2.0          # PDF report generation

# Encoding & Language Detection
chardet==5.2.0
langdetect==1.0.9

# Data Validation
pydantic==2.7.0

# Utilities
python-dotenv==1.0.1
tenacity==8.3.0           # Retry with exponential backoff

# Testing
pytest==8.2.2
pytest-mock==3.14.0

# Security
bandit==1.7.9

# ─────────────────────────────────────────────────────────────────────────────
# Data Source Connectors (install only what you use — lazy-loaded)
# ─────────────────────────────────────────────────────────────────────────────

# Database Connectors
psycopg2-binary            # PostgreSQL
pymysql                    # MySQL
pymongo                    # MongoDB
pyodbc                     # SQL Server (requires unixODBC on macOS: brew install unixodbc)

# Cloud Storage Connectors
boto3                      # AWS S3 + Secrets Manager
azure-storage-file-datalake # Azure ADLS Gen2
google-cloud-storage        # Google Cloud Storage

# Data Warehouse Connectors
snowflake-connector-python  # Snowflake
google-cloud-bigquery       # BigQuery
databricks-sql-connector    # Databricks

# ─────────────────────────────────────────────────────────────────────────────
# RAG & Vector Store
# ─────────────────────────────────────────────────────────────────────────────
chromadb                   # Vector store (requires sqlite3 >= 3.35)
sentence-transformers      # Local embeddings (optional — ChromaDB uses its own by default)

# ─────────────────────────────────────────────────────────────────────────────
# Analytics & Visualization
# ─────────────────────────────────────────────────────────────────────────────
plotly                     # Interactive charts on Module 06

# ─────────────────────────────────────────────────────────────────────────────
# Secret Manager (optional production dependencies)
# ─────────────────────────────────────────────────────────────────────────────
# hvac                     # HashiCorp Vault (uncomment if using Vault)

